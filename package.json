{
  "name": "stake-learn",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web",
    "install:all": "npm install && npm install --workspace=packages/sdk"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "firebase": "^12.4.0",
    "firebase-admin": "^13.5.0"
  }
}
